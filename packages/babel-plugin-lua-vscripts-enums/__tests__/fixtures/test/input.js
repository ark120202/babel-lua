const a = OverheadAlerts_t.OVERHEAD_ALERT_GOLD;
const b = modifierfunction.MODIFIER_PROPERTY_PREATTACK_BONUS_DAMAGE;
const c = DOTADamageFlag_t[Math.random() > 0.5 ? 'DOTA_DAMAGE_FLAG_IGNORES_MAGIC_ARMOR' : 'DOTA_DAMAGE_FLAG_NONE'];
